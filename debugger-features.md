# デバッガの機能

この章では、典型的なデバッガが備えている機能をひととおり並べ、それぞれが
「何のためにあるのか」を説明します。ここで名前を覚えておくと、
第 6 章からの実装で「いま作っているのはこの機能だ」と地図の上で位置を確認できます。
機能の名前づけは `gdb`（GNU プロジェクトの代表的なデバッガ）の用語にだいたい従います[](#cite:stallman2023)。

## ブレークポイント

デバッガのすべての出発点は、**実行を止める**ことです。
止まってさえいれば、あとはゆっくり中身を調べられます。
逆に止められなければ、プログラムは一瞬で最後まで走り抜けてしまい、何も観察できません。

指定した場所に来たら実行を止める、という仕掛けを
[ブレークポイント](#index:ブレークポイント) (breakpoint) と呼びます。
もっとも一般的なのは「ソースコードの何行目に来たら止める」という行ブレークポイントです。

```text
(dbg) break 42        # 42 行目に来たら止める
(dbg) continue        # ブレークポイントに当たるまで実行
stop at line 42
```

ブレークポイントには、いくつかの発展形があります。

- [条件付きブレークポイント](#index:条件付きブレークポイント) (conditional breakpoint):
  「42 行目に来て、かつ `i == 100` のときだけ止める」のように、条件を付けられるもの。
  1 万回まわるループの、特定の 1 回だけを捕まえたいときに役立ちます。
- 関数ブレークポイント: 「`fib` という関数に入るたびに止める」もの。
- [ウォッチポイント](#index:ウォッチポイント) (watchpoint): 場所ではなく値に着目し、
  「ある変数の値が変わったら止める」もの。データウォッチポイントとも呼びます。

これらは「[](what-is-debugging.md)」で挙げた問い（「ここに来たら」「この値が変わったら」教えてほしい）に直接対応しています。
行ブレークポイントとステップ実行は第 6 章で、条件付きブレークポイントと
ウォッチポイントは第 8 章で実装します。

## ステップ実行

止めたあと、こんどは**少しずつ前へ進めたい**ことがよくあります。
1 行実行しては状態を確認し、また 1 行……という進め方を
[ステップ実行](#index:ステップ実行) (stepping) と呼びます。
ステップ実行には、関数呼び出しの扱いによって、ふつう 3 つの種類があります。

| コマンド | 通称 | 関数呼び出しに出会ったとき |
|----------|------|------------------------------|
| step in  | ステップイン | 呼び出し先の関数の中へ**入って**、その先頭で止まる |
| step over| ステップオーバー | 呼び出し先を**最後まで一気に実行**し、戻ってきた次の行で止まる |
| step out | ステップアウト | いまの関数を**最後まで実行**し、呼び出し元に戻ったところで止まる |

なぜ 3 種類も必要なのでしょうか。たとえば `z = add(x, y)` という 1 行を考えます。
`add` の中身が怪しいなら、中に入って調べたい（ステップイン）。
`add` は信頼できるので結果だけ見たいなら、一気に飛ばしたい（ステップオーバー）。
いまいる関数にはもう用がなく、呼び出し元に戻りたいなら、抜け出したい（ステップアウト）。
この 3 つを使い分けることで、調べたい部分にだけ注意を集中できます。

> [!NOTE]
> 「1 行」とは何か、というのは実は自明ではありません。
> 1 つの行に複数の文が書かれていることもあれば、1 つの式が複数行にまたがることもあります。
> どの単位で止まるかは処理系が「行」をどう区切るかに依存します。
> 本書の題材言語では「1 文 = 1 行」と単純化し、文の切れ目で止まることにします（第 5 章）。

## 変数の検査とバックトレース

止めたら、状態を覗きます。覗きたい状態は大きく 2 種類あります。

ひとつは [変数](#index:変数の検査) の値です。
いま見えている変数（ローカル変数や引数）が、それぞれどんな値を持っているかを表示します。
多くのデバッガでは、単に表示するだけでなく、その場で任意の式を評価できます。
`gdb` の `print` コマンドや、Ruby の `debug.gem` の `p` コマンドがこれにあたります。
「`x` の値」だけでなく「`x * 2 + y` の値」をその場で計算できる、というわけです。

もうひとつは [コールスタック](#index:コールスタック) (call stack) です。
関数が関数を呼び、その関数がまた別の関数を呼ぶ……という呼び出しの連なりを、
一覧で見せてくれる機能を [バックトレース](#index:バックトレース) (backtrace) と呼びます。

```text
(dbg) backtrace
  add  (line 2)      ← いまここ
  main (line 8)      ← add を呼んだのはここ
```

バックトレースは「いまの場所に、どういう経路でたどり着いたのか」を教えてくれます。
バグの原因をさかのぼるとき、この経路情報はきわめて重要です。
コールスタックそのもののしくみと、その表示の実装は第 7 章でじっくり扱います。

## 再開とジャンプ

止めて、覗いたら、また動かしたい。実行を再開する操作も立派な機能です。

- 再開 (continue): 次のブレークポイントまで一気に走らせる。
- 値の書き換え: 止めた場所で変数の値を変更してから再開する。
  「[](what-is-debugging.md)」でいう「もし、ここで違う値だったら？」という実験を、
  プログラムを書き換えずに試せます。

さらに高度なデバッガでは、実行する場所そのものを飛ばしたり、
実行を巻き戻したりすることさえできます。「さっきの状態に戻ってやり直す」という
逆方向の実行は [リバースデバッグ](#index:リバースデバッグ) (reverse debugging) と呼ばれ、
これも本書の後半（第 8 章と第 9 章）で取り上げる魅力的なテーマです。

## 周辺の補助機能

最後に、上記を支える周辺機能を挙げます。これらは地味ですが、
実際に使うデバッガの使い勝手を大きく左右します。

- **ソース表示 (list)**: いま止まっている場所の前後のソースコードを表示する。
  どこにいるのかを目で確認できると、格段に作業しやすくなります。
- **REPL／式評価**: 止めた場所の文脈で、任意の式を評価して結果を見る。
  [REPL](#index:REPL) は Read-Eval-Print Loop（読み取り、評価、表示の繰り返し）の略で、
  入力を受け取って評価し、結果を返す対話的なループのことです。
  デバッガのプロンプトは、まさにこの REPL になっています。
- **逆アセンブルとメモリ表示**: 機械語レベルのデバッガでは、命令やメモリの中身を
  生の形で見られます。これは次章で扱う「デバッガの種類」と深く関わります。

> [!IMPORTANT]
> ここに挙げた機能は、けっして魔法ではありません。
> どれも「実行を止める」「状態を読む」「状態を書く」「実行を進める」という
> 4 つの基本操作の組み合わせにすぎません。
> 第 6 章以降で、この 4 つを私たちの題材処理系の上に実装していくと、
> 一見複雑に見えた機能が、驚くほど素直なコードで実現できることが分かるはずです。

次章では、ここで整理した機能をどういう立場でどういう実装方式で提供するのか、
という観点から、デバッガの種類を見ていきます。
