# デバッガの種類

前章では「どんな機能があるか」を見ました。この章では「どんな**種類**のデバッガがあるか」を
整理します。同じ「ブレークポイント」でも、機械語の世界で実現するのと、
インタプリタの内部で実現するのとでは、しくみがまるで違います。
種類を知っておくと、第 9 章で本物のデバッガのしくみを読むときに迷子になりません。

## 観察する高さ：ソースレベルか、機械語レベルか

ひとつめの分類軸は、**どの高さ（抽象度）でプログラムを観察するか**です。

[ソースレベルデバッガ](#index:ソースレベルデバッガ) (source-level debugger) は、
私たちが書いたソースコードの言葉で実行を見せてくれます。
「42 行目で止まった」「変数 `sum` は `15`」のように、人間が書いたとおりの行番号や
変数名で対話できます。今日ふつうに使うデバッガはほとんどこれです。

[機械語レベルデバッガ](#index:機械語レベルデバッガ) (machine-level debugger) は、
CPU が実際に実行する [機械語](#index:機械語)（CPU が直接解釈する命令の列）や
[レジスタ](#index:レジスタ)（CPU 内部の小さな記憶場所）、生のメモリの中身を見せてくれます。
ソースコードがない場合や、コンパイラの出力そのものを疑うときに使います。

ここで自然な疑問がわきます。コンパイルされて機械語になってしまったプログラムを、
どうやって「ソースコードの言葉」で見せられるのでしょうか。
答えは、コンパイラが**対応表**を一緒に出力しているからです。
「この機械語の命令は、ソースの何行目に対応する」「このメモリ位置は、変数 `sum` だ」という
対応情報を [デバッグ情報](#index:デバッグ情報) (debug information) と呼びます。
Linux などで広く使われるデバッグ情報の標準形式が [DWARF](#index:DWARF) です[](#cite:dwarf2017)。
ソースレベルデバッガとは、機械語の世界の出来事を、このデバッグ情報を使って
ソースの世界へ翻訳して見せてくれる道具だ、とも言えます。
ただし、この翻訳がいつも完璧とは限りません。コンパイラが最適化を行うと、
命令の並べ替えや変数の削除によってソースとの対応が崩れ、行が飛んで見えたり
変数が表示できなくなったりします（第 9 章で `<optimized out>` として再登場します）。
DWARF の中身は第 9 章でもう少し覗きます。

> [!NOTE]
> 私たちがこの本で作るのは、インタプリタの内部に組み込むソースレベルデバッガです。
> 自作の処理系では、行番号も変数名も処理系自身が知っているので、
> DWARF のような外部の対応表を用意する必要がありません。
> ここが、自作処理系でデバッガを学ぶことのいちばんの「お得」なところです。

## 実装方式：プロセスの外からか、処理系の中からか

ふたつめの軸は、**デバッガが対象プログラムをどこから制御するか**です。
これは本書の核心に直結します。

### プロセス制御型（外から制御する）

ひとつめの方式は、対象プログラムを独立した [プロセス](#index:プロセス)（OS が管理する、
実行中のプログラムの単位）として走らせ、デバッガが**別のプロセスとして外側から**
それを制御するものです。`gdb` や `lldb` がこの代表です。

このとき OS（基本ソフト）の助けが必要になります。Linux では
[ptrace](#index:ptrace) という [システムコール](#index:システムコール) が使われます。
システムコールとは、プログラムが OS に仕事を頼むための呼び出し口のことです。
`ptrace` を使うと、あるプロセスが別のプロセスの実行を止めたり、
レジスタやメモリを読み書きしたりできるようになります。
外から他人のプロセスの中身をいじる、強力でちょっと危ない仕組みです。
詳しくは第 9 章で説明します。

### インタプリタ統合型（中から制御する）

ふたつめの方式は、デバッガを**言語処理系そのものの内部に組み込む**ものです。
インタプリタは、もともとプログラムを 1 ステップずつ自分の手で実行しています。
だったら、その「1 ステップ実行するループ」に、「ここで止まる？ 変数を見せる？」という
チェックを差し込むだけで、デバッガになります。OS の特別な力は要りません。

Ruby の `debug.gem` はこの方式です。Ruby インタプリタが持つ
[TracePoint](#index:TracePoint) という、実行中のイベント（行の実行、メソッド呼び出しなど）を
横から受け取る仕組みを使って、実行を観察・制御します[](#cite:sasada2021)。

```mermaid
graph TD
    subgraph プロセス制御型
      A[gdb プロセス] -->|ptrace| B[対象プロセス]
    end
    subgraph インタプリタ統合型
      C[インタプリタ + デバッガ] -->|内部のフック| C
    end
```

**本書が作るのは、このインタプリタ統合型です。**
自作インタプリタの実行ループにフック（差し込み口）を 1 つ用意するだけで、
ブレークポイントもステップ実行も実現できます。第 6 章でその手軽さを体験します。

## 観察のタイミング：前向きか、巻き戻せるか

みっつめの軸は、**時間方向に進むだけか、戻れるか**です。

ふつうのデバッガは、実行を一方向（前向き）にしか進められません。
「行きすぎた！ さっきの状態をもう一度見たい」と思っても、
プログラムを最初から走らせ直すしかありません。

これに対して、実行中の出来事をすべて記録しておき、あとから前にも後ろにも
自由に移動できるようにしたデバッガがあります。Lewis はこれを
[全知デバッガ](#index:全知デバッガ) (omniscient debugger) と名づけました[](#cite:lewis2003)。
プログラムの実行中に起きたすべての状態変化を記録しておけば、
「いつでも好きな時点の状態を見られる」というアイデアです。

似て非なるアプローチに [レコード&リプレイ](#index:レコードリプレイ) (record and replay) があります。
これは実行を一度記録し、あとで**まったく同じ実行を何度でも再現**する技術です。
`rr` というツールは、低いオーバーヘッド（実行を記録するための追加コスト）で
実用的なプログラムの記録・再生を実現し、巻き戻しデバッグの基盤になりました[](#cite:ocallahan2017)。
「毎回は再現しない、気まぐれなバグ」を一度つかまえて何度も調べられる点が画期的でした。

本書では第 8 章で、このアイデアの一番素朴な形——実行の各ステップの状態を全部記録しておき、
あとから一覧する全知デバッガ——を、自作処理系の上で実装してみます。
インタプリタの中にいれば、状態の記録はおどろくほど簡単だからです。

## 動く場所：手元か、遠隔か

最後の軸は、デバッガと対象が**同じ場所にいるか**です。

スマートフォンや組み込み機器、サーバーの中で動くプログラムは、
手元のパソコンの画面に直接デバッガを表示できないことがあります。
そこで、対象側に小さな「エージェント」だけを置き、手元のデバッガと
ネットワーク越しに通信する方式が使われます。これを
[リモートデバッグ](#index:リモートデバッグ) (remote debugging) と呼びます。

近年は、デバッガとエディタ（VS Code など）の間の通信を標準化した
[DAP](#index:DAP) (Debug Adapter Protocol) という共通規約が普及しました。
DAP のおかげで、ひとつのエディタからさまざまな言語のデバッガを、
同じ操作感で扱えるようになっています。Ruby の `debug.gem` も DAP に対応しており、
リモートデバッグや VS Code 連携を実現しています[](#cite:rubydebug)。
DAP のような「デバッガとフロントエンドを分離する」設計思想は第 9 章で改めて触れます。

> [!TIP]
> ここで挙げた 4 つの軸（観察の高さ・制御の場所・時間方向・距離）は、
> 互いに独立した分類です。たとえば「ソースレベルで・インタプリタ統合型で・全知で・リモート」な
> デバッガもありえます。実在のデバッガを見かけたら、この 4 軸のどこに位置するかを
> 当てはめてみると、その正体がすっきり整理できます。

地図はこれで描けました。次章からは、いよいよ手を動かします。
まず、デバッグの対象となる小さな言語処理系を Ruby で作りましょう。
