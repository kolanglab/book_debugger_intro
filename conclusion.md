# おわりに

## 私たちがたどった道

本書は「デバッガとは何か」という問いから出発しました。
そしてデバッガを、**他のプログラムの実行を観察し、制御する道具**と定義しました。
この最小の定義を出発点に、私たちは次の道のりを歩いてきました。

- デバッグとは推測ではなく、観察と実験にもとづく体系的な作業であること
  （[](what-is-debugging.md)）。
- デバッガの機能（ブレークポイント、ステップ実行、変数の検査、バックトレース）は、
  「止める・読む・書く・進める」という 4 つの基本操作の組み合わせにすぎないこと
  （[](debugger-features.md)）。
- デバッガには、観察の高さ・制御の場所・時間方向・距離という分類軸があること
  （[](debugger-types.md)）。
- Ruby で小さなインタプリタ Toy を作り、その実行ループに `on_line` という
  フックを 1 つ用意したこと（[](toy-language.md)）。
- そのフックから、ブレークポイント・ステップ実行・変数検査・フレーム移動・
  条件付きブレークポイント・ウォッチポイント・実行履歴の記録までを生やしたこと
  （[](breakpoints-and-stepping.md) 〜 [](advanced-features.md)）。
- そして、その経験を足がかりに、`gdb` や `debug.gem` といった本物のデバッガの
  しくみを「規模の大きな同じ仕事」として読み解いたこと（[](real-world-debuggers.md)）。

いちばん伝えたかったのは、**デバッガはブラックボックスではない**ということです。
あなたはもう、自分の手でデバッガを動かしました。
その経験は、これから出会うどんなデバッガを理解するときにも効いてきます。


## 言語処理系とデバッガは一心同体

本書のもうひとつの主題は、「言語処理系を作ると、その言語のためのデバッガが作れる」
ということでした。実際にやってみて分かったのは、それが**おまけ**ではなく、
処理系の内部構造から自然に生まれてくる、ということです。

行番号はどこから来たか。構文解析のときに付けた `[:stmt, 行番号, ...]` の札からでした。
変数の値はどこにあったか。フレームの `env` という表の中でした。
コールスタックはどこにあったか。関数呼び出しで積み降ろしする `@frames` そのものでした。
デバッガが見せる情報は、すべて処理系がもともと内部に持っていたものです。
デバッガとは、その内部情報に窓を開ける作業だったのです。

> [!NOTE]
> だからこそ、新しい言語を設計・実装するときは、最初からデバッグのことを
> 考えておくと幸せになれます。行番号を AST にきちんと残す、実行を途中で観察できる
> 構造にしておく。こうした小さな配慮が、あとで強力なデバッガを生む土壌になります。

## 発展の方向

本書で作ったのは、ごく小さなデバッガでした。ここから先には、たくさんの面白い道が続いています。
腕試しに、次のような拡張に挑戦してみてください。

- **ステップアウトの実装**: 第 6 章で触れたとおり、基準の深さを 1 つ浅くするだけで作れます。
- **変数の書き換え**: 止めた場所で `set x = 99` のように値を変え、実行を続ける。
  「[](what-is-debugging.md)」で述べた「もし違う値だったら？」という実験ができるようになります。
- **リバースステップ**: 第 8 章の実行履歴の上に「1 つ戻る」コマンドをかぶせる。
- **文字列や配列の導入**: 整数だけだった Toy 言語を拡張し、より複雑なデータを覗けるようにする。
- **対話画面の改良**: ソースを色付きで表示したり、止まった行を常に表示したりする。

実装の幅を広げたくなったら、まず言語処理系そのものをもっと知るのが近道です。
『Crafting Interpreters』は、一冊で本格的な言語を二通りの方式で実装する名著で、
本書の次の一歩に最適です[](#cite:nystrom2021)。理論的な土台を固めたいなら、
古典のドラゴンブック[](#cite:aho2006) や、評価器の本質を美しく描く
『計算機プログラムの構造と解釈』[](#cite:abelson1996) が長く役立ちます。

## デバッグの技を磨く

道具を作る話とは別に、**道具を使いこなす**技術もまた、奥が深い世界です。
どんなに高機能なデバッガも、使い手の考え方が雑だと力を発揮しません。

Agans の『Debugging』は、9 つのシンプルな規則として、デバッグの心構えを説きます[](#cite:agans2002)。
「思い込みを疑え」「一度に一つだけ変えろ」といった原則は、道具を超えて通用します。
より体系的・科学的にデバッグを学びたいなら、Zeller の『Why Programs Fail』が、
仮説と実験のサイクルとしてデバッグを定式化してくれます[](#cite:zeller2009)。

そして、巻き戻しデバッグのような最先端に触れたいなら、全知デバッガの原論文[](#cite:lewis2003) や、
それを実用化した `rr` の論文[](#cite:ocallahan2017) を読んでみてください。
本書の第 8 章で作った素朴な記録デバッガが、どこまで発展しうるのかが見えてきます。
身近な実装例としては、Ruby の `debug.gem`[](#cite:rubydebug) のソースコードを読むのも、
本書で得た知識を本物のコードに接続するよい教材になります。

## 全知デバッガの記録と圧縮

「巻き戻しデバッグ」と聞いて最初に気になるのは、「全部覚えていたら、メモリがいくらあっても足りないのでは？」という点でしょう。
これはまさに正しい直感で、全知デバッガ（ODB, Omniscient Debugger）の原論文（Bil Lewis の
「Debugging Backwards in Time」[](#cite:lewis2003)）が取り組んだのも、この「いかに効率よく記録し、圧縮するか」という問題でした。
ここでは、本書で作った素朴な記録デバッガと対比しながら、その工夫を読み解いてみます。

### 工夫 1：スナップショットではなく「変化」を記録する

本書の `RecordingDebugger` は、1 行ごとに変数表をまるごと複製（`env.dup`）して保存していました。
分かりやすい反面、**変わっていない変数まで毎回コピーする**ため、記録量は「ステップ数 × 状態の大きさ」に膨らみます。

ODB の発想は逆です。状態の写真（スナップショット）を撮るのではなく、
「いつ・どこで・どの変数（やフィールド）が・どんな値になったか」という個々の変化（イベント）だけを、
連番のタイムスタンプつきの小さなレコードとして 1 件ずつ書き留めます。
変わったものしか記録しないので、これは実質的に差分ログであり、素朴なスナップショット方式よりはるかにコンパクトになります。

### 工夫 2：状態は「保存」せず「復元」する

では、ある時点の変数の値はどうやって知るのでしょうか。ODB はそれを保存しておくのではなく、
**イベント列をさかのぼって計算します**。「この時刻より前で、その変数に最後に書き込まれた値」を探せばよいのです。
保存は最小限にとどめ、参照されたときに必要な分だけ再構成する、という割り切りです。
時間を行き来できるのも、この「イベント列を前後にたどる」操作の延長線上にあります。

> [!NOTE]
> 本書の方式が「先に全部覚えておく」なら、ODB は「記録は変化だけ、状態は問われたら作る」。
> 同じ「過去を観察する」目的でも、どこにコストを払うかが違うわけです。

### 工夫 3：それでも残る「メモリの壁」

それでもイベントは実行とともに増え続けます。原論文の ODB は記録を基本的にメモリ上に持つため、
長時間動くプログラムでは結局この壁にぶつかります（原論文自身、これを率直に課題として認めています）。

この壁を越える決定打が、第 8 章でも触れた**レコード&リプレイ**です。
`rr`[](#cite:ocallahan2017) は「すべての状態を覚える」のではなく、
同じ実行を再現するのに必要な最小限の情報（入力やスレッド切り替えなどの非決定的な要素）だけを記録し、
あとは再実行することで任意の時点の状態を作り直します。
「覚える」より「やり直す」ほうが安い、という見立ての逆転です。

身近な到達点を見たければ、Ruby の `debug.gem`[](#cite:rubydebug) には記録・再生に関する機能があり、
原論文のアイデアが現役の道具にどう根づいているかをコードで確かめられます。
こうして眺めると、本書の `env.dup` 一行から、原論文の差分記録、そして実用デバッガまでが、
「過去を観察する」という一本の道でつながっていることが見えてくるはずです。

## 最後に

プログラムは思ったとおりには動かず、書いたとおりに動きます。
その「書いたとおり」を、ありのままに見せてくれるのがデバッガでした。
そしてあなたは、その「ありのままを見せる窓」を、自分の手で作る方法を知りました。

これからデバッガを使うとき、ふと「いまこの中では `on_line` のようなものが
呼ばれているのだな」と想像できたら、本書の目的は十分に果たされたことになります。
よいデバッグの旅を。
