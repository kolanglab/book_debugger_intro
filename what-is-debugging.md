# デバッグという仕事

デバッガという道具を作る前に、その道具が支えようとしている「デバッグ」という作業を
もう少し解像度を上げて眺めておきましょう。
道具は、作業を助けるために存在します。どんな作業を助けたいのかが分かっていないと、
どんな機能を持つべきかも決まりません。

## デバッグの方法

バグが起きているとき、私たちの頭の中ではたいてい次のようなことが起きています。
「ここが悪いはずだ」と思って直す。直らない。「じゃあここだ」と思って直す。直らない……。
この当てずっぽうの繰り返しは、デバッグでもっとも時間を浪費するパターンです。

Zeller は、効果的なデバッグを「仮説 → 実験 → 観察 → 修正された仮説」という
科学的方法のサイクルとして定式化しました[](#cite:zeller2009)。
つまりデバッグとは次のような営みです。

1. **観察**: プログラムが実際に示している、おかしな振る舞いを正確にとらえる。
2. **仮説**: 「この変数がこの時点で負の値になっているせいではないか」のように、
   原因についての検証可能な推測を立てる。
3. **実験**: その仮説が正しければこうなるはず、という予測を立て、実際に確かめる。
4. **判定**: 予測と観察が一致すれば仮説は支持され、ずれていれば仮説を捨てて立て直す。

この 4 つのうち、観察と実験を強力に支えるのがデバッガです。
「この変数の、この瞬間の値が見たい」という観察も、「ここで止めて、
変数を書き換えてから続きを動かしたらどうなる？」という実験も、デバッガがあれば
ソースコードを書き換えずに何度でも試せます。
Agans のいう「思い込みを疑い、実際に見よ」という原則[](#cite:agans2002) を、
道具のレベルで実現したものがデバッガなのだ、と言ってもよいでしょう。

## 症状と原因の距離

デバッグが難しい最大の理由は、**バグの症状が現れる場所と、本当の原因がある場所が、
プログラムの中で離れている**ことです。Zeller はこの連鎖を、欠陥 (defect) が
感染 (infection) を生み、それが伝播して最終的に失敗 (failure) として観測される、
という流れで説明しています[](#cite:zeller2009)。

具体的に考えてみましょう。たとえば次のような流れです。

- 100 行目で、ある変数にうっかり間違った値を代入してしまう（これが**原因**）。
- その間違った値が、関数の引数として渡され、別の変数にコピーされ……と伝わっていく。
- 最終的に 500 行目で、画面に変な数字が表示される（これが**症状**）。

私たちが最初に気づくのは 500 行目の症状です。しかし直すべきなのは 100 行目です。
この「症状から原因へさかのぼる」探索を効率よくやるには、
プログラムの実行のどの時点で値がおかしくなったのかを突き止める必要があります。
これはまさに、実行を止めて値を覗くというデバッガの仕事そのものです。

> [!NOTE]
> 症状から原因へさかのぼるとき、「正しかった最後の地点」と「すでに壊れている最初の地点」を
> 二分探索的に絞り込む、という戦略がよく効きます。
> 実行を途中で止めて状態を確認できるデバッガは、この絞り込みと相性が抜群です。

## プリントデバッグとその限界

もっとも手軽なデバッグ手法は、すでに触れた [プリントデバッグ](#index:プリントデバッグ) です。
気になる場所に値を表示する文を埋め込みます。Ruby ならこうです。

```ruby
def total_price(items)
  sum = 0
  items.each do |item|
    sum = sum + item.price
    puts "DEBUG: item=#{item.name} sum=#{sum}"   # ← 埋め込んだ観察用の文
  end
  sum
end
```

プリントデバッグには、はっきりした長所があります。特別な道具がいらず、どんな環境でも使え、
プログラムの実行を妨げません。実際、熟練したプログラマでも日常的に使う有効な手段です。

しかし限界もあります。第一に、**観察したい場所を変えるたびにソースコードを書き換えて、
プログラムを動かし直さなければなりません**。第二に、表示する文を消し忘れて
本番のコードに残してしまう事故が起きがちです。第三に、表示したい対象が
あらかじめ予想できるものに限られます。「あの変数とこの変数、どっちが先におかしくなった？」
のような、その場で生まれる疑問にすばやく答えるのは苦手です。

デバッガは、この限界を超えるための道具です。ソースコードを一切書き換えずに、
実行を任意の場所で止め、その場でどんな変数でも覗き、必要なら書き換え、
また走らせることができます。観察したい対象を、走らせる前に決めておく必要がありません。
止まったその場で、思いついた質問を投げかけられるのです。

ひとつだけ心に留めておいてほしいことがあります。観察という行為そのものが、
対象の挙動を変えることがあるのです。ブレークポイントで止めれば実行のタイミングが変わり、
タイミングに依存するバグ（スレッドの競合など）が再現しなくなるかもしれません。
プリントデバッグの 1 行ですら、実行を遅くしたり出力の順序を変えたりします。
観察すると消えてしまうバグは**ハイゼンバグ**（Heisenbug）と呼ばれ、
デバッグでは「道具が対象に与える影響」も仮説の一部として考える必要があります。

> [!TIP]
> プリントデバッグとデバッガは、どちらが優れているという関係ではありません。
> 「ループを 1 万回まわした全体の傾向を一覧したい」ときはログ（プリント）が向き、
> 「特定の 1 回だけ深く調べたい」ときはデバッガが向きます。
> 道具の特性を知って使い分けるのが、結局いちばん速いのです。

## デバッガが助ける典型的な問いかけ

ここまでをまとめると、デバッガは次のような問いに、ソースを書き換えずに答える道具です。

- 「いま、どこを実行しているのか？」 → 現在の実行位置を示す。
- 「ここに来たとき、変数はどんな値？」 → 任意の地点で実行を止め、変数を覗く。
- 「ここまで、どういう順で関数が呼ばれてきたのか？」 → 呼び出しの履歴をたどる。
- 「この値が変わった瞬間を教えてほしい」 → 値の変化を監視して止める。
- 「もし、ここで違う値だったら？」 → 止めた場所で状態を書き換えて先へ進める。

次章では、これらの問いに答えるためにデバッガが備える具体的な機能を、
ひとつずつ名前をつけて整理していきます。
そこで登場する用語（ブレークポイント、ステップ実行、バックトレースなど）は、
このあと本書を通じて、私たち自身の手で実装していく対象でもあります。
